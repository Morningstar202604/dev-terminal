*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab

# 测试产物：保留报告与截图，忽略可再生的视频片段/录屏大文件
test_results/promo_clips/
test_results/promo_cards/
test_results/**/recording/
test_results/**/ui_recording/
test_results/**/*.webm

# 说明：Python 运行时（Pyodide/WASM，约 13MB）随仓库提供，位于
# app/src/main/assets/pyodide/，由 AAPT 打进 APK，clone 后即可离线构建。
#
# design/ 下的原型需要同一份运行时做浏览器端演示，但为避免重复存 13MB，
# 该目录不入库，用 tools/sync_design_runtime.sh 从 App assets 同步过来。
design/pyodide/full/

# Node demo 依赖（本地 symlink，不入库）
node_modules/

# 构建/发布产物（APK、视频、报告）走 Release，不入库
dist/
交付/
