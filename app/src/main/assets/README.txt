# 说明：把离线工具链放在此目录
#
# 期望文件名：usrtar.zip
# 生成方式见 tools/extract_bootstrap.sh（真机 Termux）或
#          tools/build_offline_bundle.sh（CI 交叉编译）
#
# 结构应为（对应 termux $PREFIX）：
#   bin/python, bin/java, bin/javac, lib/, share/ ...
#
# 注意：本文件仅作占位说明，构建时可删除。
